# Rail-Network-Reachability-Analyser
Utilising Python to achieve O(n log n) time complexity using union-find with path compression and union by rank, reducing naive O(n^2) DSU operations to effectively O(n), achieving 100% pass rate on test model
